<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="utf-8">
	<title>User Index</title>
	<style>
		* {
			font-family: Helvetica, Arial, sans-serif;
		}

		div {
			display: flex;
			align-items: center;
			padding: 1em;
			font-size: 1.2em;
			background-color: #eee;
			margin-bottom:1em;
		}

		div a {
			padding-left: 0.5em;
		}
	</style>
</head>

<body>
	<h1>We are using Handlebars!</h1>
	{{#each users as |user|}}
		<div>
			<img src="/profilepics/{{user.username}}_sm.jpg" alt="{{user.username}}">
			<a href="/{{user.username}}">{{user.name.full}}</a>
		</div>
	{{/each}}
</body>

</html>
